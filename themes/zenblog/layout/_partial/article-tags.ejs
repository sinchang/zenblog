<% if (item.tags && item.tags.length){ %>
<%
    var tags = [];
    item.tags.forEach(function(tag){
        tags.push('<a href="' + config.root + tag.path + '"><i class="fa fa-tag"></i>' + tag.name + '</a>');
    });
%>
<div class="blog-tags-container">
    <%- tags.join(' ') %>
</div>
<% } %>
